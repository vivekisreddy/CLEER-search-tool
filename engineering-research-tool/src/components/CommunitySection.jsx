import { Card, CardContent, Typography } from "@mui/material";

const CommunitySection = () => {
    return (
        <Card>
            <CardContent>
                <Typography variant="h6">Community Recommendations</Typography>
                <Typography variant="body2">See what other educators are reading...</Typography>
            </CardContent>
        </Card>
    );
};

export default CommunitySection;
